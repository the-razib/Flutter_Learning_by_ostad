import 'package:flutter/material.dart';

class Notificationfragment extends StatelessWidget {
  const Notificationfragment({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Notificationfragment'),
      ),
    );
  }
}
