import 'package:flutter/material.dart';

class Videofragmnet extends StatelessWidget {
  const Videofragmnet({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Videofragmnet'),
      ),
    );
  }
}
